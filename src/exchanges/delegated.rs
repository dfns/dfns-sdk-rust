// Code generated by rust-sdk-generator. DO NOT EDIT.

#[allow(unused_imports)]
use super::types::*;

/// Delegated client for exchanges operations. Signed operations are split into
/// init/complete pairs so the challenge can be signed on the user side.
#[derive(Clone)]
pub struct DelegatedExchangesClient {
    client: crate::client::Client,
}

impl DelegatedExchangesClient {
    pub fn new(client: crate::client::Client) -> Self {
        DelegatedExchangesClient { client }
    }

    /// Retrieve the details of a specific exchange integration configuration.
    pub async fn get_exchange(
        &self,
        exchange_id: String,
    ) -> Result<GetExchangeResponse, crate::error::Error> {
        let path = format!("/exchanges/{}", urlencoding::encode(&exchange_id));
        self.client
            .request::<GetExchangeResponse>(reqwest::Method::GET, &path, None, false)
            .await
    }

    /// Starts delegated signing for deleteExchange: returns the challenge to sign.
    /// Pass the signed assertion to delete_exchange_complete with the same arguments.
    pub async fn delete_exchange_init(
        &self,
        exchange_id: String,
    ) -> Result<crate::signer::UserActionChallenge, crate::error::Error> {
        let path = format!("/exchanges/{}", urlencoding::encode(&exchange_id));
        self.client
            .create_user_action_challenge(reqwest::Method::DELETE, &path, None)
            .await
    }

    /// Finishes delegated signing for deleteExchange: submits the signed challenge
    /// and issues the request.
    pub async fn delete_exchange_complete(
        &self,
        exchange_id: String,
        challenge_identifier: String,
        assertion: crate::signer::CredentialAssertion,
    ) -> Result<DeleteExchangeResponse, crate::error::Error> {
        let path = format!("/exchanges/{}", urlencoding::encode(&exchange_id));
        let user_action = self
            .client
            .complete_user_action_signing(challenge_identifier, &assertion)
            .await?;
        self.client
            .request_with_user_action::<DeleteExchangeResponse>(
                reqwest::Method::DELETE,
                &path,
                None,
                &user_action,
            )
            .await
    }

    /// List all configured exchange integrations.
    pub async fn list_exchanges(
        &self,
        query: Option<ListExchangesQuery>,
    ) -> Result<ListExchangesResponse, crate::error::Error> {
        let mut path = String::from("/exchanges");
        if let Some(query) = &query {
            let mut q: Vec<String> = Vec::new();
            if let Some(v) = &query.limit {
                q.push(format!("limit={}", urlencoding::encode(&v.to_string())));
            }
            if let Some(v) = &query.pagination_token {
                q.push(format!(
                    "paginationToken={}",
                    urlencoding::encode(&v.to_string())
                ));
            }
            if !q.is_empty() {
                path.push('?');
                path.push_str(&q.join("&"));
            }
        }
        self.client
            .request::<ListExchangesResponse>(reqwest::Method::GET, &path, None, false)
            .await
    }

    /// Starts delegated signing for createExchange: returns the challenge to sign.
    /// Pass the signed assertion to create_exchange_complete with the same arguments.
    pub async fn create_exchange_init(
        &self,
        body: CreateExchangeRequest,
    ) -> Result<crate::signer::UserActionChallenge, crate::error::Error> {
        let path = String::from("/exchanges");
        let body = serde_json::to_value(&body)?;
        self.client
            .create_user_action_challenge(reqwest::Method::POST, &path, Some(&body))
            .await
    }

    /// Finishes delegated signing for createExchange: submits the signed challenge
    /// and issues the request.
    pub async fn create_exchange_complete(
        &self,
        body: CreateExchangeRequest,
        challenge_identifier: String,
        assertion: crate::signer::CredentialAssertion,
    ) -> Result<CreateExchangeResponse, crate::error::Error> {
        let path = String::from("/exchanges");
        let body = serde_json::to_value(&body)?;
        let user_action = self
            .client
            .complete_user_action_signing(challenge_identifier, &assertion)
            .await?;
        self.client
            .request_with_user_action::<CreateExchangeResponse>(
                reqwest::Method::POST,
                &path,
                Some(&body),
                &user_action,
            )
            .await
    }

    /// Get a list of accounts for a specific exchange.
    pub async fn list_accounts(
        &self,
        exchange_id: String,
        query: Option<ListAccountsQuery>,
    ) -> Result<ListAccountsResponse, crate::error::Error> {
        let mut path = format!("/exchanges/{}/accounts", urlencoding::encode(&exchange_id));
        if let Some(query) = &query {
            let mut q: Vec<String> = Vec::new();
            if let Some(v) = &query.limit {
                q.push(format!("limit={}", urlencoding::encode(&v.to_string())));
            }
            if let Some(v) = &query.pagination_token {
                q.push(format!(
                    "paginationToken={}",
                    urlencoding::encode(&v.to_string())
                ));
            }
            if !q.is_empty() {
                path.push('?');
                path.push_str(&q.join("&"));
            }
        }
        self.client
            .request::<ListAccountsResponse>(reqwest::Method::GET, &path, None, false)
            .await
    }

    /// Retrieve the list of assets for a specific account on a specific exchange.
    pub async fn list_account_assets(
        &self,
        exchange_id: String,
        account_id: String,
        query: Option<ListAccountAssetsQuery>,
    ) -> Result<ListAccountAssetsResponse, crate::error::Error> {
        let mut path = format!(
            "/exchanges/{}/accounts/{}/assets",
            urlencoding::encode(&exchange_id),
            urlencoding::encode(&account_id)
        );
        if let Some(query) = &query {
            let mut q: Vec<String> = Vec::new();
            if let Some(v) = &query.limit {
                q.push(format!("limit={}", urlencoding::encode(&v.to_string())));
            }
            if let Some(v) = &query.pagination_token {
                q.push(format!(
                    "paginationToken={}",
                    urlencoding::encode(&v.to_string())
                ));
            }
            if !q.is_empty() {
                path.push('?');
                path.push_str(&q.join("&"));
            }
        }
        self.client
            .request::<ListAccountAssetsResponse>(reqwest::Method::GET, &path, None, false)
            .await
    }

    /// Lists the networks to which the given asset can be withdrawn from an exchange account.
    pub async fn list_asset_withdrawal_networks(
        &self,
        exchange_id: String,
        account_id: String,
        asset: String,
    ) -> Result<serde_json::Value, crate::error::Error> {
        let path = format!(
            "/exchanges/{}/accounts/{}/assets/{}/withdrawal-networks",
            urlencoding::encode(&exchange_id),
            urlencoding::encode(&account_id),
            urlencoding::encode(&asset)
        );
        self.client
            .request::<serde_json::Value>(reqwest::Method::GET, &path, None, false)
            .await
    }

    /// Starts delegated signing for createDeposit: returns the challenge to sign.
    /// Pass the signed assertion to create_deposit_complete with the same arguments.
    pub async fn create_deposit_init(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateDepositRequest,
    ) -> Result<crate::signer::UserActionChallenge, crate::error::Error> {
        let path = format!(
            "/exchanges/{}/accounts/{}/deposits",
            urlencoding::encode(&exchange_id),
            urlencoding::encode(&account_id)
        );
        let body = serde_json::to_value(&body)?;
        self.client
            .create_user_action_challenge(reqwest::Method::POST, &path, Some(&body))
            .await
    }

    /// Deprecated: use `create_deposit_init` instead.
    #[deprecated(note = "use `create_deposit_init` instead")]
    pub async fn create_exchange_deposit_init(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateDepositRequest,
    ) -> Result<crate::signer::UserActionChallenge, crate::error::Error> {
        self.create_deposit_init(exchange_id, account_id, body)
            .await
    }

    /// Finishes delegated signing for createDeposit: submits the signed challenge
    /// and issues the request.
    pub async fn create_deposit_complete(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateDepositRequest,
        challenge_identifier: String,
        assertion: crate::signer::CredentialAssertion,
    ) -> Result<CreateDepositResponse, crate::error::Error> {
        let path = format!(
            "/exchanges/{}/accounts/{}/deposits",
            urlencoding::encode(&exchange_id),
            urlencoding::encode(&account_id)
        );
        let body = serde_json::to_value(&body)?;
        let user_action = self
            .client
            .complete_user_action_signing(challenge_identifier, &assertion)
            .await?;
        self.client
            .request_with_user_action::<CreateDepositResponse>(
                reqwest::Method::POST,
                &path,
                Some(&body),
                &user_action,
            )
            .await
    }

    /// Deprecated: use `create_deposit_complete` instead.
    #[deprecated(note = "use `create_deposit_complete` instead")]
    pub async fn create_exchange_deposit_complete(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateDepositRequest,
        challenge_identifier: String,
        assertion: crate::signer::CredentialAssertion,
    ) -> Result<CreateDepositResponse, crate::error::Error> {
        self.create_deposit_complete(
            exchange_id,
            account_id,
            body,
            challenge_identifier,
            assertion,
        )
        .await
    }

    /// Starts delegated signing for createWithdrawal: returns the challenge to sign.
    /// Pass the signed assertion to create_withdrawal_complete with the same arguments.
    pub async fn create_withdrawal_init(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateWithdrawalRequest,
    ) -> Result<crate::signer::UserActionChallenge, crate::error::Error> {
        let path = format!(
            "/exchanges/{}/accounts/{}/withdrawals",
            urlencoding::encode(&exchange_id),
            urlencoding::encode(&account_id)
        );
        let body = serde_json::to_value(&body)?;
        self.client
            .create_user_action_challenge(reqwest::Method::POST, &path, Some(&body))
            .await
    }

    /// Deprecated: use `create_withdrawal_init` instead.
    #[deprecated(note = "use `create_withdrawal_init` instead")]
    pub async fn create_exchange_withdrawal_init(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateWithdrawalRequest,
    ) -> Result<crate::signer::UserActionChallenge, crate::error::Error> {
        self.create_withdrawal_init(exchange_id, account_id, body)
            .await
    }

    /// Finishes delegated signing for createWithdrawal: submits the signed challenge
    /// and issues the request.
    pub async fn create_withdrawal_complete(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateWithdrawalRequest,
        challenge_identifier: String,
        assertion: crate::signer::CredentialAssertion,
    ) -> Result<CreateWithdrawalResponse, crate::error::Error> {
        let path = format!(
            "/exchanges/{}/accounts/{}/withdrawals",
            urlencoding::encode(&exchange_id),
            urlencoding::encode(&account_id)
        );
        let body = serde_json::to_value(&body)?;
        let user_action = self
            .client
            .complete_user_action_signing(challenge_identifier, &assertion)
            .await?;
        self.client
            .request_with_user_action::<CreateWithdrawalResponse>(
                reqwest::Method::POST,
                &path,
                Some(&body),
                &user_action,
            )
            .await
    }

    /// Deprecated: use `create_withdrawal_complete` instead.
    #[deprecated(note = "use `create_withdrawal_complete` instead")]
    pub async fn create_exchange_withdrawal_complete(
        &self,
        exchange_id: String,
        account_id: String,
        body: CreateWithdrawalRequest,
        challenge_identifier: String,
        assertion: crate::signer::CredentialAssertion,
    ) -> Result<CreateWithdrawalResponse, crate::error::Error> {
        self.create_withdrawal_complete(
            exchange_id,
            account_id,
            body,
            challenge_identifier,
            assertion,
        )
        .await
    }
}
